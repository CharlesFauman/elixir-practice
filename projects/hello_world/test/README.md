`test_helper.exs` will help us run all of the ExUnit tests

by convension, test files use the original file name appended by `_test`,
and use `.exs` instead of `.ex` to indicate that they are not to be compiled.